import * as g from "@akashic/akashic-engine";
import { Platform } from "../Platform";
import { CanvasSurface } from "./CanvasSurface";
export declare module RenderingHelper {
    function toTextFromCompositeOperation(operation: g.CompositeOperation): string;
    function toCompositeOperationFromText(operationText: string): g.CompositeOperation;
    function drawSystemTextByContext2D(context: CanvasRenderingContext2D, text: string, x: number, y: number, maxWidth: number, fontSize: number, textAlign: g.TextAlign, textBaseline: g.TextBaseline, textColor: string, fontFamily: g.FontFamily, strokeWidth: number, strokeColor: string, strokeOnly: boolean): void;
    function createPrimarySurface(width: number, height: number, rendererCandidates?: string[]): CanvasSurface;
    function createBackSurface(width: number, height: number, platform: Platform, rendererCandidates?: string[]): g.Surface;
}
