import * as g from "@akashic/akashic-engine";
import { WebAudioAsset } from "./WebAudioAsset";
import { AudioManager } from "../../AudioManager";
import { AudioPlayer } from "../AudioPlayer";
export declare class WebAudioPlayer extends g.AudioPlayer implements AudioPlayer {
    _endedEventHandler: () => void;
    private _gainNode;
    private _sourceNode;
    private _audioContext;
    private _manager;
    private _dummyDurationWaitTimer;
    constructor(system: g.AudioSystem, manager: AudioManager);
    changeVolume(volume: number): void;
    _changeMuted(muted: boolean): void;
    play(asset: WebAudioAsset): void;
    stop(): void;
    notifyMasterVolumeChanged(): void;
    private _onAudioEnded;
    private _clearEndedEventHandler;
    private _calculateVolume;
}
