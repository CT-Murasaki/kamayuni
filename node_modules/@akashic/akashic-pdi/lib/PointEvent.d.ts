import * as g from "@akashic/akashic-engine";
export declare const enum PointType {
    Down = 0,
    Move = 1,
    Up = 2,
}
export interface PointEvent {
    type: PointType;
    identifier: number;
    offset: g.CommonOffset;
}
