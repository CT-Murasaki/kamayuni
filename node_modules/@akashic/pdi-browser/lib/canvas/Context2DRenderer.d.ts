import * as g from "@akashic/akashic-engine";
import { CanvasSurface } from "./CanvasSurface";
export declare class Context2DRenderer extends g.Renderer {
    private surface;
    private context;
    constructor(surface: CanvasSurface, context: CanvasRenderingContext2D);
    clear(): void;
    drawImage(surface: g.Surface, offsetX: number, offsetY: number, width: number, height: number, canvasOffsetX: number, canvasOffsetY: number): void;
    drawSprites(surface: g.Surface, offsetX: number[], offsetY: number[], width: number[], height: number[], canvasOffsetX: number[], canvasOffsetY: number[], count: number): void;
    drawSystemText(text: string, x: number, y: number, maxWidth: number, fontSize: number, textAlign: g.TextAlign, textBaseline: g.TextBaseline, textColor: string, fontFamily: g.FontFamily, strokeWidth: number, strokeColor: string, strokeOnly: boolean): void;
    translate(x: number, y: number): void;
    transform(matrix: number[]): void;
    opacity(opacity: number): void;
    save(): void;
    restore(): void;
    fillRect(x: number, y: number, width: number, height: number, cssColor: string): void;
    setCompositeOperation(operation: g.CompositeOperation): void;
    setOpacity(opacity: number): void;
    setTransform(matrix: number[]): void;
}
