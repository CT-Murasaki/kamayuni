/**
 * `GameLoop` が描画を行う基準。
 */
declare enum LoopRenderMode {
    /**
     * 毎raw frame後に描画する。
     * raw frameの詳細についてはClock.tsのコメントを参照。
     */
    AfterRawFrame = 0,
    /**
     * 描画をまったく行わない。
     */
    None = 1
}
export default LoopRenderMode;
