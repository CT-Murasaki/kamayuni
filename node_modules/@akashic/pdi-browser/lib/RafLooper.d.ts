import { Looper } from "@akashic/akashic-pdi";
export declare class RafLooper implements Looper {
    _fun: (deltaTime: number) => number;
    _timerId: number;
    _prev: number;
    constructor(fun: (deltaTime: number) => number);
    start(): void;
    stop(): void;
}
